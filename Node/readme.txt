create package.json

npm init
 then give the project name (project name cannot contain capital letters)

install packages
npm install

update packages
npm update

to use modules we need to add "type": "module", to package.json